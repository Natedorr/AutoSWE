/.vs
**/__pycache__/**
/worktrees/
/data/
/running/
/logs/
/.venv/
.claude/

/config/autoswe.env
/config/repos.json
/config/harnesses.json

# Local timestamped backups (*.bak-YYYYMMDD[-suffix]) — never commit; they
# routinely contain live PATs/tokens from config files.
*.bak-*

# Review-phase run artifacts (local outputs, like /logs/ and /data/)
/artifacts/

# Coverage output
.coverage

# Manual test helpers — output files
tests/helpers/ado_post_summary.json
